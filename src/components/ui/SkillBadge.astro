---
/**
 * SkillBadge — pixel-styled badge with subtle glow on hover.
 */
interface Props {
  name: string;
  category?: string;
}

const { name, category } = Astro.props;
---

<span class="inline-flex items-center px-3 py-1.5 font-mono text-xs font-medium bg-[var(--bg-elevated)] text-[var(--accent)] border border-[var(--border-default)] hover:border-[var(--accent)] hover:bg-[var(--accent-muted)] hover:shadow-[0_0_8px_var(--accent-muted)] transition-all">
  {name}
</span>
